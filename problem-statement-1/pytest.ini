
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Default options for local development (headed mode for debugging)
# CI overrides these with --headed=false --slowmo=0 for headless execution
addopts = -v -s --headed --slowmo=500
